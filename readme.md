# TimeScale CLI

Benchmarking tool

## Usage

You should have docker-compose and docker installed, then run

```bash
cd timescale-cli
```
then

```bash
docker-compose up -d
```
After that

```bash
./timescale-cli -file=query_params.csv
```